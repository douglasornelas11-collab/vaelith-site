const content=require('./content');const {SITE,esc,shell}=require('./layout');
module.exports=function renderArticle(slug){const a=content.articles.find(x=>x.slug===slug);if(!a)return null;const related=content.articles.filter(x=>x.categorySlug===a.categorySlug&&x.slug!==a.slug).slice(0,3);const side=related.map(x=>`<div class="sidebar-card"><a href="/noticias/${esc(x.slug)}"><div class="tag">${esc(x.category)}</div><h4>${esc(x.title)}</h4><div class="meta"><span>${esc(x.dateDisplay)}</span></div></a></div>`).join('');const paragraphs=a.body.map(p=>`<p>${esc(p)}</p>`).join('');const translated=a.sourceLanguage&&a.sourceLanguage!=='Português';const canonical=`${SITE}/noticias/${a.slug}`;const ld=JSON.stringify({'@context':'https://schema.org','@type':'NewsArticle',headline:a.title,description:a.dek,image:[a.image],datePublished:a.date,dateModified:a.date,mainEntityOfPage:canonical,author:{'@type':'Organization',name:'VAELITH Editorial'},publisher:{'@type':'Organization',name:'VAELITH',url:SITE}});const body=`<main class="article-shell"><div class="container"><div class="breadcrumb"><a href="/">Início</a> / <a href="/categoria/${esc(a.categorySlug)}">${esc(a.category)}</a> / Notícia</div><div class="article-grid"><article><header class="article-header"><div class="tag">${esc(a.category)} · ${esc(a.region)}</div><h1>${esc(a.title)}</h1><p class="article-dek">${esc(a.dek)}</p><div class="article-info"><span>VAELITH Editorial</span><span>${esc(a.dateDisplay)}</span><span>${esc(a.category)} · ${esc(a.region)}</span><span>Fonte: ${esc(a.sourceName)}</span>${translated?'<span>Fonte internacional · contextualizada em português</span>':''}</div></header><div class="article-hero" style="background-image:url('${esc(a.image)}')"></div><div class="article-body">${paragraphs}<div class="insight"><strong>Leitura VAELITH</strong><p>Esta matéria é uma síntese editorial própria baseada na fonte identificada. O objetivo é oferecer contexto técnico ao setor brasileiro sem reproduzir o texto original.</p></div></div><div class="source-box"><h3>Fonte e transparência</h3><p>Fonte principal: <strong>${esc(a.sourceName)}</strong>. ${translated?'A reportagem original está em outro idioma; a VAELITH produziu uma síntese em português e adicionou contexto editorial.':'A VAELITH produziu uma síntese própria e adicionou contexto editorial.'}</p><a class="source-link" href="${esc(a.sourceUrl)}" target="_blank" rel="noopener noreferrer">Consultar fonte original ↗</a></div></article><aside class="sidebar"><div class="sidebar-title">Mais em ${esc(a.category)}</div>${side}<div class="source-box"><h3>Correções</h3><p>Encontrou alguma informação que precisa de ajuste?</p><a class="source-link" href="mailto:labsvaelith@gmail.com">Falar com o editorial →</a></div></aside></div></div></main>`;return shell({title:a.title,description:a.dek,canonical,active:a.categorySlug,body,jsonLd:ld,ogImage:a.image});}
