const content=require('../lib/content');const {SITE}=require('../lib/layout');
const x=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');
module.exports=async(req,res)=>{await content.refreshFromSanity();const now=Date.now();const items=[...content.articles].filter(a=>{const ts=new Date(a.publishedAt||`${a.date}T00:00:00-03:00`).getTime();return Number.isFinite(ts)&&ts<=now;}).sort((a,b)=>String(b.publishedAt||b.date).localeCompare(String(a.publishedAt||a.date))).slice(0,20).map(a=>{const url=`${SITE}/noticias/${a.slug}`;const pubDate=a.publishedAt||`${a.date}T00:00:00-03:00`;const pub=new Date(pubDate).toUTCString();return `<item><title>${x(a.title)}</title><link>${x(url)}</link><guid isPermaLink="true">${x(url)}</guid><pubDate>${pub}</pubDate><category>${x(a.category)}</category><description>${x(a.dek)}</description></item>`}).join('\n');const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<rss version="2.0"><channel><title>VAELITH</title><link>${SITE}/</link><description>Engenharia, arquitetura, construção, infraestrutura, gestão e tecnologia.</description><language>pt-BR</language><lastBuildDate>${new Date().toUTCString()}</lastBuildDate>${items}</channel></rss>`;res.setHeader('Content-Type','application/rss+xml; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=900, stale-while-revalidate=86400');res.statusCode=200;res.end(xml);};
