const renderHome=require('../lib/render-home');const renderArticle=require('../lib/render-article');const pages=require('../lib/render-pages');const {SITE,shell}=require('../lib/layout');
module.exports=(req,res)=>{const type=String(req.query.type||'home'),slug=String(req.query.slug||''),q=String(req.query.q||'');let out=null;if(type==='home')out=renderHome();else if(type==='article')out=renderArticle(slug);else if(type==='category')out=pages.category(slug);else if(type==='events')out=pages.events();else if(type==='event')out=pages.events(slug);else if(type==='about')out=pages.info('about');else if(type==='editorial')out=pages.info('editorial');else if(type==='privacy')out=pages.info('privacy');else if(type==='author')out=pages.author();else if(type==='search')out=pages.search(q);if(!out){res.statusCode=404;out=shell({title:'Página não encontrada',description:'Página não encontrada',canonical:SITE,body:'<main class="empty"><div class="container"><div class="kicker">404</div><h1>Página não encontrada</h1><p>O conteúdo pode ter sido movido ou ainda não está disponível.</p><p><a class="source-link" href="/">Voltar para a página inicial →</a></p></div></main>'});}else res.statusCode=200;res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=300, stale-while-revalidate=86400');res.end(out);};
